model:
  # Update these paths to a specific trained run.
  path: models/rf/rf_20260210_001650/model.joblib
  feature_order: models/rf/rf_20260210_001650/feature_order.json

data:
  dir: /mnt/c/Users/amehedi/Downloads/ml_debris/output/thomas
  target_name: dem_diff.tif

permutation:
  n_repeats: 10
  max_samples: 50000
  scoring: r2
  random_state: 42
  n_jobs: 1
  top_k: 20

partial_dependence:
  # Set to null/empty to use first max_features from feature_order.
  features:
    - burn__severity
    - cation__exchange_capacity
    - clay__total
    - dry__bulk_density
    - field__capacity
    - landcover
    - pH
    - porosity
    - sand__total
    - saturated__water_content
    - silt__total
    - soil__density
    - soil__internal_friction_angle
    - soil__maximum_total_cohesion
    - soil__minimum_total_cohesion
    - soil__mode_total_cohesion
    - soil__saturated_hydraulic_conductivity
    - soil__texture
    - soil__thickness
    - soil__transmissivity
    - topographic__elevation
    - vegetation__plant_functional_type
    - wilting__point
  max_features: 23
  grid_resolution: 50
  max_samples: 50000
  random_state: 42

output:
  # If omitted, defaults to experiments/interpret/<run_id>
  dir: experiments/interpret/manual_run
